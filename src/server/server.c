#include "server.h"

int setup(){
    // set_signal_handlers();
    printf("Signal handlers set\n");
    // create_message_queue();
    // init semaphores
    // sem_init(&mutex_state, 0, 1);
    return 0;
}